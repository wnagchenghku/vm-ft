

3. portno is hard coded.

util/qemu-option.c:
opts_do_parse

4. add explanations about ./configure, no as needed, LD_LIBRARY_PATH (shared library)

To examine how symbols are resolved runtime: export LD_DEBUG=all.

extern bsymbolic functions
